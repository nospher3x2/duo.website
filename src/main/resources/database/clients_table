create table duo_clients (
      id INTEGER NOT NULL AUTO_INCREMENT,
      name VARCHAR(60) NOT NULL,
      email VARCHAR(255) NOT NULL,
      cpf VARCHAR(11) NOT NULL,
      password VARCHAR(255) NOT NULL,
      purchasedPlugins TEXT,

      primary key (id)
  );